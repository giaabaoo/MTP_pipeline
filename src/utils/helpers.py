import os.path as osp
import os
import pdb

# def mkdir(path):
#     os.makedirs(path, exist_ok=True)

# def setup_dir(cfg):
    # setup output dir
    # next_dir = osp.join(cfg.output_dir, cfg.data.output_path)
    # mkdir(next_dir)
    
    # # setup video_description_output_path dir
    # next_dir = osp.join(cfg.output_dir, cfg.data.output_video_description_path)
    # mkdir(next_dir)
    
    # # setup video_summary_output_path dir
    # next_dir = osp.join(cfg.output_dir, cfg.data.output_video_summary_path)
    # mkdir(next_dir)